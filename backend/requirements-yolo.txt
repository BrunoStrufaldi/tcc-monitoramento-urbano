# Dependências opcionais para a inferência YOLO real.
# Instale apenas na máquina que processará imagens: pip install -r requirements-yolo.txt
-r requirements.txt
ultralytics>=8.3.0
Pillow>=10.0.0

# Só necessário na máquina que TREINA o modelo de incidentes
# (ml/train_incident_model.py), não na que só faz inferência.
roboflow>=1.1.0
